---
description: Show model delegation tiers, models, and rules
---

Run this command and relay its output verbatim to the user:

```bash
node "${CLAUDE_PLUGIN_ROOT:-.}/hooks/scripts/cli.mjs" tiers
```
